■Bladeチートシート
@section(str) 〜 @endsection 箇所に名前を定義
@yield(str)	@sectionで名付けた箇所の読み込み
	@@parent  名付けた箇所にオーバライドではなく追加
@include        他のビューの取り込み

@each("blade.template", $hoge, "hoge")
 部分テンプレートをコレクション要素でループ
第四引数は引数の配列が空のとき。

@inject('var', 'App\Service\hogeService')
サービスコンテナからサービスを取得


■変数
{{$hoge}} 変数の表示　*htmentities 関数を通す
@{{ name }} 波括弧のエスケープ
{!! hoge !!} 変数の表示 (関数を通さない）

■関数
{{ time() }} 関数の実行
{{ $hoge or "default"} nullの時代わりの文字列を表示

■制御構文
@if @elseif @else @endif
@unless @endunless   @if( empty($hoge))と等価
@for($i = 0; $i<10; $i++) @endfor
@foreach ($hoges as $hoge) @endforeach
@forelse($users as $user ) @empty @endforelse
@while( $hoge < $hage ) @endwhile

